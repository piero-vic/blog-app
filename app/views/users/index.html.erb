<ul class="flex flex-col gap-5">
  <% @users.each do |user| %>
    <li>
      <a href=<%= "/users/#{user.id}" =%>>
        <%= render partial: "shared/user_info", locals: { user: user } %>
      </a>
    </li>
  <% end %>
</ul>
